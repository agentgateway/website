> [!NOTE]
> The published metrics record zero failed requests across all measured stages
> for the Kubernetes Service baseline and both agentgateway modes. Each treatment
> has only one repetition, so these results do not quantify run-to-run variability.
