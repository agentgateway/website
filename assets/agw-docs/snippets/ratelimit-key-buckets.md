Each rule keeps up to 65,536 buckets and drops the least used ones, which for that key is the same as never having been seen. Buckets are held in memory by a single {{< conditional-text include-if="kubernetes" >}}proxy replica, so each replica{{< /conditional-text >}}{{< conditional-text include-if="standalone" >}}agentgateway instance, so each instance{{< /conditional-text >}} enforces the limit separately.
