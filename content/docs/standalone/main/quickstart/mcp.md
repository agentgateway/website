---
title: MCP servers
weight: 12
description: Connect to an MCP server and try tools in the agentgateway playground.
# The 1.3 quickstart adds the MCP server through the UI (Enable MCP -> Add server), which
# is not scriptable, so the doc test is skipped here. The 1.2 (latest) version keeps the
# config-file flow and its test.
test: skip
---

{{< reuse "agw-docs/standalone/quickstart/mcp.md" >}}
